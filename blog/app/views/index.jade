extends blogpost 

block content
  - var posts = getPosts(0,5)
  - var page  = 1
  - each post in posts
    div.container
      div.post
        h1 #{post.title}
        div.preview 
          != post.preview
